diff --git a/CMakeLists.txt b/CMakeLists.txt
index d453a64b..c5477b5f 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,235 +1,250 @@
-﻿cmake_minimum_required(VERSION 3.15...4.2)
-
-# ── Generator enforcement ─────────────────────────────────────────────────────
-# VSEPR-SIM exclusively uses Ninja.  CMakePresets.json declares this for every
-# preset; the guard below catches bare `cmake -S . -B build` invocations that
-# bypass presets so they never silently fall back to Make or Visual Studio.
-if(NOT CMAKE_GENERATOR MATCHES "Ninja")
-    message(FATAL_ERROR
-        "VSEPR-SIM requires the Ninja generator.\n"
-        "Configure with a preset:  cmake --preset release\n"
-        "Or pass explicitly:       cmake -G Ninja -S . -B build\n"
-        "Ninja is at: C:/msys64/ucrt64/bin/ninja.exe")
-endif()
-
-project(vsepr-sim VERSION 5.14.1 LANGUAGES C CXX)
-
-set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -static -static-libgcc -static-libstdc++")
-
-# ============================================================================
-# VSEPR-SIM  Two-Part Build System (Orchestrator)
-# ============================================================================
-#
-# This root file provides:
-#   1. Project identity, compiler settings, build options
-#   2. include(cmake/CoreBuild.cmake)  Part 1: all headless targets
-#   3. include(cmake/VisBuild.cmake)   Part 2: VIS / GUI / Desktop
-#   4. Tests (after both parts so all targets are available)
-#   5. Summary
-#
-# Branch: 4.0.4.03
-# Lineage: kernel v0.1 â†’ v0.3 â†’ user 2.7 â†’ 2.9 â†’ 3.0 â†’ 4.0-LB
-# Purpose: Multi-scale property search with mature deterministic kernel
-#
-# Previous audit checkpoint: 4.0.0
-# Original monolithic file archived at:
-#   archive/CMakeLists.txt.monolithic-backup
-# ============================================================================
-
-# Global include paths for project headers
-include_directories(${PROJECT_SOURCE_DIR}/src ${PROJECT_SOURCE_DIR}/include)
-
-# ============================================================================
-# Optional CUDA Support (graceful fallback to CPU)
-# ============================================================================
-
-include(CheckLanguage)
-check_language(CUDA)
-if(CMAKE_CUDA_COMPILER)
-    enable_language(CUDA)
-    set(VSEPR_HAS_CUDA ON)
-    message(STATUS "CUDA enabled: ${CMAKE_CUDA_COMPILER}")
-else()
-    set(VSEPR_HAS_CUDA OFF)
-    message(STATUS "CUDA not found, using CPU fallback")
-endif()
-
-# ============================================================================
-# Build Options
-# ============================================================================
-
-option(BUILD_TESTS "Build test suite" ON)
-option(BUILD_APPS  "Build applications" ON)
-option(BUILD_VIS   "Build visualization tools (vsepr-view, vsepr_vis)" ON)
-option(BUILD_ADVANCED_RENDER "Build advanced 3D molecular renderer + data window (requires BUILD_VIS=ON)" OFF)
-option(BUILD_DEMOS "Build demo targets" OFF)
-option(BUILD_BGFX_DEMO "Build the standalone BGFX persistent-window demo" OFF)
-option(BUILD_VIEWER        "Build lightweight one-shot file viewer (WO-67-A1, requires BUILD_VIS=ON)" ON)
-option(BUILD_VIEWER_DAEMON "Build persistent viewer daemon (WO-67-A2, requires BUILD_VIS=ON)" OFF)
-
-# ── VSEPR Feature flags ───────────────────────────────────────────────────────
-option(VSEPR_ENABLE_ATOMISTIC     "Enable atomistic molecular simulation layer" ON)
-option(VSEPR_ENABLE_COARSE_GRAIN  "Enable coarse-grain reduced representation layer" ON)
-option(VSEPR_ENABLE_XYZ_IO        "Enable XYZ/XYZFull parsing and validation" ON)
-option(VSEPR_ENABLE_FIRE          "Enable FIRE minimization" ON)
-option(VSEPR_ENABLE_THERMO        "Enable thermodynamics module" ON)
-option(VSEPR_ENABLE_REACTION      "Enable reaction discovery module" ON)
-option(VSEPR_ENABLE_CRYSTAL_LIB   "Enable crystal benchmark library" ON)
-option(VSEPR_ENABLE_REGISTRY_CORE "Enable VSIM registry/default resolver" ON)
-option(VSEPR_ENABLE_INSTALL_TOOLS "Enable install/doctor/path tools" ON)
-
-# ── Generate vsepr_build_config.hpp ──────────────────────────────────────────
-list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
-include(VSEPRBuildInfo)
-vsepr_generate_build_config(OUTPUT_DIR "${CMAKE_BINARY_DIR}/generated")
-
-# ============================================================================
-# Compiler Settings
-# ============================================================================
-# C++23 (N4950) â€” upgraded from C++20 for std::expected, monadic optional,
-# std::unreachable, std::to_underlying, consteval improvements.
-# GCC 15.2.0 (UCRT64) supports the full C++23 feature set.
-# C++26 (N5014) features tracked for future adoption â€” see cpp_corpus/.
-
-set(CMAKE_CXX_STANDARD 23)
-set(CMAKE_CXX_STANDARD_REQUIRED ON)
-set(CMAKE_CXX_EXTENSIONS OFF)
-
-# M_PI and friends for all platforms (Clang/UCRT64 needs this)
-add_compile_definitions(_USE_MATH_DEFINES)
-
-if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
-    add_compile_options(-Wall -Wextra -pedantic)
-elseif(MSVC)
-    add_compile_options(/W4)
-endif()
-
-# ============================================================================
-# Part 1: Core Build (headless libraries and apps, no graphics deps)
-# ============================================================================
-
-include(cmake/CoreBuild.cmake)
-
-# ── Feature compile definitions (propagate to all targets via vsepr_core) ────
-target_compile_definitions(vsepr_core INTERFACE
-    VSEPR_ENABLE_ATOMISTIC=$<BOOL:${VSEPR_ENABLE_ATOMISTIC}>
-    VSEPR_ENABLE_COARSE_GRAIN=$<BOOL:${VSEPR_ENABLE_COARSE_GRAIN}>
-    VSEPR_ENABLE_XYZ_IO=$<BOOL:${VSEPR_ENABLE_XYZ_IO}>
-    VSEPR_ENABLE_FIRE=$<BOOL:${VSEPR_ENABLE_FIRE}>
-    VSEPR_ENABLE_THERMO=$<BOOL:${VSEPR_ENABLE_THERMO}>
-    VSEPR_ENABLE_REACTION=$<BOOL:${VSEPR_ENABLE_REACTION}>
-    VSEPR_ENABLE_CRYSTAL_LIB=$<BOOL:${VSEPR_ENABLE_CRYSTAL_LIB}>
-    VSEPR_ENABLE_REGISTRY_CORE=$<BOOL:${VSEPR_ENABLE_REGISTRY_CORE}>
-    VSEPR_ENABLE_INSTALL_TOOLS=$<BOOL:${VSEPR_ENABLE_INSTALL_TOOLS}>
-)
-
-# ============================================================================
-# Part 2: Visualization and GUI Build
-#         OpenGL / GLFW / GLEW / ImGui / Qt6
-#         Dependencies auto-fetched via FetchContent when not on system.
-# ============================================================================
-
-include(cmake/VisBuild.cmake)
-
-# ============================================================================
-# Part 3: Advanced Renderer (optional — requires BUILD_VIS=ON)
-#         3D molecular window + ImGui data panel
-# ============================================================================
-
-include(cmake/AdvancedRender.cmake)
-
-if(BUILD_BGFX_DEMO)
-    include(cmake/BgfxDemo.cmake)
-endif()
-
-# ============================================================================
-# Tests (after both parts so VIS targets are visible to test helpers)
-# ============================================================================
-
-if(BUILD_TESTS)
-    enable_testing()
-    add_subdirectory(tests)
-endif()
-
-# ============================================================================
-# Bridge Beta (scaffold for next-gen architecture prerequisites)
-# ============================================================================
-add_subdirectory(bridge_beta)
-
-# ============================================================================
-# Install bundle payload
-# ============================================================================
-
-install(FILES
-    ${PROJECT_SOURCE_DIR}/README.md
-    ${PROJECT_SOURCE_DIR}/VSIM_REFERENCE.md
-    ${PROJECT_SOURCE_DIR}/VSIM_DEVELOPMENT.md
-    ${PROJECT_SOURCE_DIR}/CMakePresets.json
-    ${PROJECT_SOURCE_DIR}/build.ps1
-    DESTINATION .
-)
-
-install(DIRECTORY ${PROJECT_SOURCE_DIR}/include/vsim
-    DESTINATION include
-)
-
-install(DIRECTORY ${PROJECT_SOURCE_DIR}/data
-    DESTINATION .
-)
-
-install(DIRECTORY ${PROJECT_SOURCE_DIR}/docs
-    DESTINATION .
-)
-
-install(DIRECTORY ${PROJECT_SOURCE_DIR}/scripts
-    DESTINATION .
-    FILES_MATCHING
-        PATTERN "*.ps1"
-        PATTERN "*.sh"
-        PATTERN "*.py"
-        PATTERN "*.md"
-)
-
-install(DIRECTORY ${PROJECT_SOURCE_DIR}/resources
-    DESTINATION .
-)
-
-# ============================================================================
-# Summary
-# ============================================================================
-
-message(STATUS "")
-message(STATUS "VSEPR-SIM v${PROJECT_VERSION} -- Build Configuration")
-message(STATUS "  Compiler:    ${CMAKE_CXX_COMPILER_ID} ${CMAKE_CXX_COMPILER_VERSION}")
-message(STATUS "  Standard:    C++${CMAKE_CXX_STANDARD}")
-message(STATUS "  Build type:  ${CMAKE_BUILD_TYPE}")
-message(STATUS "  CUDA:        ${VSEPR_HAS_CUDA}")
-message(STATUS "")
-message(STATUS "  Build targets:")
-message(STATUS "    tests:          ${BUILD_TESTS}")
-message(STATUS "    apps:           ${BUILD_APPS}")
-message(STATUS "    vis:            ${BUILD_VIS}")
-message(STATUS "    demos:          ${BUILD_DEMOS}")
-message(STATUS "    viewer (A1):    ${BUILD_VIEWER}")
-message(STATUS "    viewd  (A2):    ${BUILD_VIEWER_DAEMON}")
-message(STATUS "")
-message(STATUS "  Runtime modules:")
-message(STATUS "    atomistic:      ${VSEPR_ENABLE_ATOMISTIC}")
-message(STATUS "    coarse_grain:   ${VSEPR_ENABLE_COARSE_GRAIN}")
-message(STATUS "    xyz_io:         ${VSEPR_ENABLE_XYZ_IO}")
-message(STATUS "    fire:           ${VSEPR_ENABLE_FIRE}")
-message(STATUS "    thermodynamics: ${VSEPR_ENABLE_THERMO}")
-message(STATUS "    reaction:       ${VSEPR_ENABLE_REACTION}")
-message(STATUS "    crystal_lib:    ${VSEPR_ENABLE_CRYSTAL_LIB}")
-message(STATUS "    registry_core:  ${VSEPR_ENABLE_REGISTRY_CORE}")
-message(STATUS "    install_tools:  ${VSEPR_ENABLE_INSTALL_TOOLS}")
-message(STATUS "")
-if(VSEPR_VIS_READY)
-    message(STATUS "  VIS/GUI: active")
-else()
-    message(STATUS "  VIS/GUI: off (deps not available)")
-endif()
-message(STATUS "")
-message(STATUS "  vsepr-sim --version    -> VSEPR-SIM ${PROJECT_VERSION}")
-message(STATUS "  vsepr-sim --build-info -> full compiler/git/module report")
-message(STATUS "")
+cmake_minimum_required(VERSION 3.15...4.2)
+
+# ── Generator enforcement ─────────────────────────────────────────────────────
+# VSEPR-SIM exclusively uses Ninja.  CMakePresets.json declares this for every
+# preset; the guard below catches bare `cmake -S . -B build` invocations that
+# bypass presets so they never silently fall back to Make or Visual Studio.
+if(NOT CMAKE_GENERATOR MATCHES "Ninja")
+    message(FATAL_ERROR
+        "VSEPR-SIM requires the Ninja generator.\n"
+        "Configure with a preset:  cmake --preset release\n"
+        "Or pass explicitly:       cmake -G Ninja -S . -B build\n"
+        "Ninja is at: C:/msys64/ucrt64/bin/ninja.exe")
+endif()
+
+project(vsepr-sim VERSION 5.14.1 LANGUAGES C CXX)
+
+set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -static -static-libgcc -static-libstdc++")
+
+# ============================================================================
+# VSEPR-SIM  Two-Part Build System (Orchestrator)
+# ============================================================================
+#
+# This root file provides:
+#   1. Project identity, compiler settings, build options
+#   2. include(cmake/CoreBuild.cmake)  Part 1: all headless targets
+#   3. include(cmake/VisBuild.cmake)   Part 2: VIS / GUI / Desktop
+#   4. Tests (after both parts so all targets are available)
+#   5. Summary
+#
+# Branch: 4.0.4.03
+# Lineage: kernel v0.1 â†’ v0.3 â†’ user 2.7 â†’ 2.9 â†’ 3.0 â†’ 4.0-LB
+# Purpose: Multi-scale property search with mature deterministic kernel
+#
+# Previous audit checkpoint: 4.0.0
+# Original monolithic file archived at:
+#   archive/CMakeLists.txt.monolithic-backup
+# ============================================================================
+
+# Global include paths for project headers
+include_directories(${PROJECT_SOURCE_DIR}/src ${PROJECT_SOURCE_DIR}/include)
+
+# ============================================================================
+# Optional CUDA Support (graceful fallback to CPU)
+# ============================================================================
+
+include(CheckLanguage)
+check_language(CUDA)
+if(CMAKE_CUDA_COMPILER)
+    enable_language(CUDA)
+    set(VSEPR_HAS_CUDA ON)
+    message(STATUS "CUDA enabled: ${CMAKE_CUDA_COMPILER}")
+else()
+    set(VSEPR_HAS_CUDA OFF)
+    message(STATUS "CUDA not found, using CPU fallback")
+endif()
+
+# ============================================================================
+# Build Options
+# ============================================================================
+
+option(BUILD_TESTS "Build test suite" ON)
+option(BUILD_APPS  "Build applications" ON)
+option(BUILD_VIS   "Build visualization tools (vsepr-view, vsepr_vis)" ON)
+option(BUILD_ADVANCED_RENDER "Build advanced 3D molecular renderer + data window (requires BUILD_VIS=ON)" OFF)
+option(BUILD_DEMOS "Build demo targets" OFF)
+option(BUILD_BGFX_DEMO "Build the standalone BGFX persistent-window demo" OFF)
+option(BUILD_VIEWER        "Build lightweight one-shot file viewer (WO-67-A1, requires BUILD_VIS=ON)" ON)
+option(BUILD_VIEWER_DAEMON "Build persistent viewer daemon (WO-67-A2, requires BUILD_VIS=ON)" OFF)
+
+# ── VSEPR Feature flags ───────────────────────────────────────────────────────
+option(VSEPR_ENABLE_ATOMISTIC     "Enable atomistic molecular simulation layer" ON)
+option(VSEPR_ENABLE_COARSE_GRAIN  "Enable coarse-grain reduced representation layer" ON)
+option(VSEPR_ENABLE_XYZ_IO        "Enable XYZ/XYZFull parsing and validation" ON)
+option(VSEPR_ENABLE_FIRE          "Enable FIRE minimization" ON)
+option(VSEPR_ENABLE_THERMO        "Enable thermodynamics module" ON)
+option(VSEPR_ENABLE_REACTION      "Enable reaction discovery module" ON)
+option(VSEPR_ENABLE_CRYSTAL_LIB   "Enable crystal benchmark library" ON)
+option(VSEPR_ENABLE_REGISTRY_CORE "Enable VSIM registry/default resolver" ON)
+option(VSEPR_ENABLE_INSTALL_TOOLS "Enable install/doctor/path tools" ON)
+
+# ── Generate vsepr_build_config.hpp ──────────────────────────────────────────
+list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
+include(VSEPRBuildInfo)
+vsepr_generate_build_config(OUTPUT_DIR "${CMAKE_BINARY_DIR}/generated")
+
+# ============================================================================
+# Compiler Settings
+# ============================================================================
+# C++23 (N4950) â€” upgraded from C++20 for std::expected, monadic optional,
+# std::unreachable, std::to_underlying, consteval improvements.
+# GCC 15.2.0 (UCRT64) supports the full C++23 feature set.
+# C++26 (N5014) features tracked for future adoption â€” see cpp_corpus/.
+
+set(CMAKE_CXX_STANDARD 23)
+set(CMAKE_CXX_STANDARD_REQUIRED ON)
+set(CMAKE_CXX_EXTENSIONS OFF)
+
+# M_PI and friends for all platforms (Clang/UCRT64 needs this)
+add_compile_definitions(_USE_MATH_DEFINES)
+
+if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
+    add_compile_options(-Wall -Wextra -pedantic)
+elseif(MSVC)
+    add_compile_options(/W4)
+endif()
+
+# ============================================================================
+# Part 1: Core Build (headless libraries and apps, no graphics deps)
+# ============================================================================
+
+include(cmake/CoreBuild.cmake)
+
+# Headless producer for the isolated dual-backend 3D demonstration. The same
+# source is compiled with BUILD_VISUALIZATION by VisBuild for the OpenGL path.
+add_executable(dual-backend-3d-data apps/cg_anim_demo.cpp)
+target_link_libraries(dual-backend-3d-data PRIVATE vsepr_core)
+target_include_directories(dual-backend-3d-data PRIVATE
+    ${PROJECT_SOURCE_DIR}
+    ${PROJECT_SOURCE_DIR}/src
+    ${PROJECT_SOURCE_DIR}/include
+)
+
+if(WIN32)
+    add_executable(native-particle-demo WIN32 apps/native_particle_demo.cpp)
+    target_link_libraries(native-particle-demo PRIVATE gdi32 user32 shell32)
+endif()
+
+# ── Feature compile definitions (propagate to all targets via vsepr_core) ────
+target_compile_definitions(vsepr_core INTERFACE
+    VSEPR_ENABLE_ATOMISTIC=$<BOOL:${VSEPR_ENABLE_ATOMISTIC}>
+    VSEPR_ENABLE_COARSE_GRAIN=$<BOOL:${VSEPR_ENABLE_COARSE_GRAIN}>
+    VSEPR_ENABLE_XYZ_IO=$<BOOL:${VSEPR_ENABLE_XYZ_IO}>
+    VSEPR_ENABLE_FIRE=$<BOOL:${VSEPR_ENABLE_FIRE}>
+    VSEPR_ENABLE_THERMO=$<BOOL:${VSEPR_ENABLE_THERMO}>
+    VSEPR_ENABLE_REACTION=$<BOOL:${VSEPR_ENABLE_REACTION}>
+    VSEPR_ENABLE_CRYSTAL_LIB=$<BOOL:${VSEPR_ENABLE_CRYSTAL_LIB}>
+    VSEPR_ENABLE_REGISTRY_CORE=$<BOOL:${VSEPR_ENABLE_REGISTRY_CORE}>
+    VSEPR_ENABLE_INSTALL_TOOLS=$<BOOL:${VSEPR_ENABLE_INSTALL_TOOLS}>
+)
+
+# ============================================================================
+# Part 2: Visualization and GUI Build
+#         OpenGL / GLFW / GLEW / ImGui / Qt6
+#         Dependencies auto-fetched via FetchContent when not on system.
+# ============================================================================
+
+include(cmake/VisBuild.cmake)
+
+# ============================================================================
+# Part 3: Advanced Renderer (optional — requires BUILD_VIS=ON)
+#         3D molecular window + ImGui data panel
+# ============================================================================
+
+include(cmake/AdvancedRender.cmake)
+
+if(BUILD_BGFX_DEMO)
+    include(cmake/BgfxDemo.cmake)
+endif()
+
+# ============================================================================
+# Tests (after both parts so VIS targets are visible to test helpers)
+# ============================================================================
+
+if(BUILD_TESTS)
+    enable_testing()
+    add_subdirectory(tests)
+endif()
+
+# ============================================================================
+# Bridge Beta (scaffold for next-gen architecture prerequisites)
+# ============================================================================
+add_subdirectory(bridge_beta)
+
+# ============================================================================
+# Install bundle payload
+# ============================================================================
+
+install(FILES
+    ${PROJECT_SOURCE_DIR}/README.md
+    ${PROJECT_SOURCE_DIR}/VSIM_REFERENCE.md
+    ${PROJECT_SOURCE_DIR}/VSIM_DEVELOPMENT.md
+    ${PROJECT_SOURCE_DIR}/CMakePresets.json
+    ${PROJECT_SOURCE_DIR}/build.ps1
+    DESTINATION .
+)
+
+install(DIRECTORY ${PROJECT_SOURCE_DIR}/include/vsim
+    DESTINATION include
+)
+
+install(DIRECTORY ${PROJECT_SOURCE_DIR}/data
+    DESTINATION .
+)
+
+install(DIRECTORY ${PROJECT_SOURCE_DIR}/docs
+    DESTINATION .
+)
+
+install(DIRECTORY ${PROJECT_SOURCE_DIR}/scripts
+    DESTINATION .
+    FILES_MATCHING
+        PATTERN "*.ps1"
+        PATTERN "*.sh"
+        PATTERN "*.py"
+        PATTERN "*.md"
+)
+
+install(DIRECTORY ${PROJECT_SOURCE_DIR}/resources
+    DESTINATION .
+)
+
+# ============================================================================
+# Summary
+# ============================================================================
+
+message(STATUS "")
+message(STATUS "VSEPR-SIM v${PROJECT_VERSION} -- Build Configuration")
+message(STATUS "  Compiler:    ${CMAKE_CXX_COMPILER_ID} ${CMAKE_CXX_COMPILER_VERSION}")
+message(STATUS "  Standard:    C++${CMAKE_CXX_STANDARD}")
+message(STATUS "  Build type:  ${CMAKE_BUILD_TYPE}")
+message(STATUS "  CUDA:        ${VSEPR_HAS_CUDA}")
+message(STATUS "")
+message(STATUS "  Build targets:")
+message(STATUS "    tests:          ${BUILD_TESTS}")
+message(STATUS "    apps:           ${BUILD_APPS}")
+message(STATUS "    vis:            ${BUILD_VIS}")
+message(STATUS "    demos:          ${BUILD_DEMOS}")
+message(STATUS "    viewer (A1):    ${BUILD_VIEWER}")
+message(STATUS "    viewd  (A2):    ${BUILD_VIEWER_DAEMON}")
+message(STATUS "")
+message(STATUS "  Runtime modules:")
+message(STATUS "    atomistic:      ${VSEPR_ENABLE_ATOMISTIC}")
+message(STATUS "    coarse_grain:   ${VSEPR_ENABLE_COARSE_GRAIN}")
+message(STATUS "    xyz_io:         ${VSEPR_ENABLE_XYZ_IO}")
+message(STATUS "    fire:           ${VSEPR_ENABLE_FIRE}")
+message(STATUS "    thermodynamics: ${VSEPR_ENABLE_THERMO}")
+message(STATUS "    reaction:       ${VSEPR_ENABLE_REACTION}")
+message(STATUS "    crystal_lib:    ${VSEPR_ENABLE_CRYSTAL_LIB}")
+message(STATUS "    registry_core:  ${VSEPR_ENABLE_REGISTRY_CORE}")
+message(STATUS "    install_tools:  ${VSEPR_ENABLE_INSTALL_TOOLS}")
+message(STATUS "")
+if(VSEPR_VIS_READY)
+    message(STATUS "  VIS/GUI: active")
+else()
+    message(STATUS "  VIS/GUI: off (deps not available)")
+endif()
+message(STATUS "")
+message(STATUS "  vsepr-sim --version    -> VSEPR-SIM ${PROJECT_VERSION}")
+message(STATUS "  vsepr-sim --build-info -> full compiler/git/module report")
+message(STATUS "")
diff --git a/apps/cg_anim_demo.cpp b/apps/cg_anim_demo.cpp
index 3e71dd11..98da62c3 100644
--- a/apps/cg_anim_demo.cpp
+++ b/apps/cg_anim_demo.cpp
@@ -1,5 +1,5 @@
-/**
- * cg_anim_demo.cpp — Animated Ensemble Proxy Demo
+﻿/**
+ * cg_anim_demo.cpp  -  Animated Ensemble Proxy Demo
  *
  * Demonstrates eta relaxation and ensemble proxy evolution across four
  * bead scene families. Reuses the test scene infrastructure directly
@@ -8,15 +8,15 @@
  * states with an animated overlay cycle in CGVizViewer.
  *
  * Scene families:
- *   1. Cubic lattice (tight, 3 A)   — high cohesion, regular structure
- *   2. Cubic lattice (loose, 6 A)   — sparse, reduced cohesion
- *   3. Aligned stack cloud (bias 1) — high texture proxy (P2 alignment)
- *   4. Random cluster (N=27)        — low uniformity, high surface sensitivity
+ *   1. Cubic lattice (tight, 3 A)    -  high cohesion, regular structure
+ *   2. Cubic lattice (loose, 6 A)    -  sparse, reduced cohesion
+ *   3. Aligned stack cloud (bias 1)  -  high texture proxy (P2 alignment)
+ *   4. Random cluster (N=27)         -  low uniformity, high surface sensitivity
  *
  * Animation per scene (~14 s auto, then manual):
- *   density    → orbit  → coordination  → orbit
- *   eta/memory → orbit  → orient-order  → orbit
- *   reset camera → close
+ *   density    -> orbit  -> coordination  -> orbit
+ *   eta/memory -> orbit  -> orient-order  -> orbit
+ *   reset camera -> close
  *
  * Controls during animation:
  *   Right-drag   orbit camera
@@ -30,33 +30,36 @@
  *
  * Architecture position:
  *   test_util scene builders + runners  (test infrastructure reuse)
- *         ↓
+ *         v
  *   EnvironmentState (converged, 500 steps)
- *         ↓
+ *         v
  *   EnsembleProxySummary  (printed to terminal)
- *         ↓
- *   CGVizViewer  (animated overlay display — BUILD_VIS required)
+ *         v
+ *   CGVizViewer  (animated overlay display  -  BUILD_VIS required)
  *
  * Reference: Emergent Effective Medium Mapping specification (Suite #5)
  */
 
 #include "tests/scene_factory.hpp"
 #include "coarse_grain/analysis/ensemble_proxy.hpp"
-#include "cli/system_state.hpp"
 #include "coarse_grain/core/bead.hpp"
 
 #ifdef BUILD_VISUALIZATION
+#  include "cli/system_state.hpp"
 #  include "coarse_grain/vis/cg_viz_viewer.hpp"
 #endif
 
 #include <cmath>
 #include <cstdio>
 #include <cstring>
+#include <filesystem>
+#include <fstream>
+#include <iomanip>
 #include <string>
 #include <vector>
 
 // ============================================================================
-// Default environment parameters — mirrors default_params() in Suite #5
+// Default environment parameters  -  mirrors default_params() in Suite #5
 // ============================================================================
 
 static coarse_grain::EnvironmentParams make_params() {
@@ -74,6 +77,59 @@ static coarse_grain::EnvironmentParams make_params() {
     return p;
 }
 
+template <typename RunResult>
+static void write_demo_artifacts(const std::filesystem::path& output_dir,
+                                 const std::vector<RunResult>& results,
+                                 int n_steps,
+                                 double dt) {
+    std::filesystem::create_directories(output_dir);
+
+    std::ofstream scenes(output_dir / "scenes.csv");
+    std::ofstream particles(output_dir / "particles.csv");
+    std::ofstream manifest(output_dir / "manifest.json");
+    if (!scenes || !particles || !manifest) {
+        throw std::runtime_error("Unable to create dual-backend demo artifacts in " + output_dir.string());
+    }
+
+    scenes.imbue(std::locale::classic());
+    particles.imbue(std::locale::classic());
+    scenes << std::setprecision(12);
+    particles << std::setprecision(12);
+
+    scenes << "scene_id,scene_name,particle_count,cohesion_proxy,texture_proxy,"
+              "stabilization_proxy,density_mean,coordination_mean\n";
+    particles << "scene_id,particle_id,x,y,z,radius,state_value\n";
+
+    for (size_t scene_id = 0; scene_id < results.size(); ++scene_id) {
+        const auto& result = results[scene_id];
+        std::string scene_name = result.name;
+        for (size_t pos = 0; (pos = scene_name.find('"', pos)) != std::string::npos; pos += 2)
+            scene_name.insert(pos, 1, '"');
+
+        scenes << scene_id << ",\"" << scene_name << "\"," << result.scene.size() << ','
+               << result.proxy.cohesion_proxy << ',' << result.proxy.texture_proxy << ','
+               << result.proxy.stabilization_proxy << ',' << result.proxy.mean_rho << ','
+               << result.proxy.mean_C << '\n';
+
+        for (size_t particle_id = 0; particle_id < result.scene.size(); ++particle_id) {
+            const auto& position = result.scene[particle_id].position;
+            particles << scene_id << ',' << particle_id << ','
+                      << position.x << ',' << position.y << ',' << position.z << ','
+                      << 0.45 << ',' << result.states[particle_id].eta << '\n';
+        }
+    }
+
+    manifest << "{\n"
+             << "  \"schema\": \"vsepr.dual_backend_3d.v1\",\n"
+             << "  \"generator\": \"cg-anim-demo\",\n"
+             << "  \"steps\": " << n_steps << ",\n"
+             << "  \"dt_fs\": " << dt << ",\n"
+             << "  \"scene_count\": " << results.size() << ",\n"
+             << "  \"scenes_csv\": \"scenes.csv\",\n"
+             << "  \"particles_csv\": \"particles.csv\"\n"
+             << "}\n";
+}
+
 // ============================================================================
 // Demo scene descriptor
 // ============================================================================
@@ -102,9 +158,10 @@ static std::vector<DemoScene> build_demo_scenes() {
 }
 
 // ============================================================================
-// Convert SceneBead + EnvironmentState vectors → CGSystemState
+// Convert SceneBead + EnvironmentState vectors -> CGSystemState
 // ============================================================================
 
+#ifdef BUILD_VISUALIZATION
 static vsepr::cli::CGSystemState make_cg_state(
     const std::string&                              name,
     const std::vector<test_util::SceneBead>&        scene,
@@ -128,6 +185,7 @@ static vsepr::cli::CGSystemState make_cg_state(
     }
     return cg;
 }
+#endif
 
 // ============================================================================
 // Terminal output helpers
@@ -172,7 +230,7 @@ static coarse_grain::vis::VizConfig make_anim_config() {
     cfg.window_width    = 1280;
     cfg.window_height   = 800;
 
-    // Overlay cycle: density → coordination → eta/memory → orient-order
+    // Overlay cycle: density -> coordination -> eta/memory -> orient-order
     // Each overlay held for 2.5 s, camera orbits 0.4 rad between each.
     cfg.commands = {
         CV::set_overlay(OM::Density),
@@ -210,21 +268,24 @@ static coarse_grain::vis::VizConfig make_anim_config() {
 
 int main(int argc, char** argv) {
     bool headless = false;
+    std::filesystem::path output_dir = "out/dual_backend_3d";
     for (int i = 1; i < argc; ++i) {
         if (std::strcmp(argv[i], "--headless") == 0) headless = true;
+        if (std::strcmp(argv[i], "--output") == 0 && i + 1 < argc) output_dir = argv[++i];
         if (std::strcmp(argv[i], "--help") == 0 ||
             std::strcmp(argv[i], "-h") == 0) {
-            std::printf("Usage: cg-anim-demo [--headless] [--help]\n");
+            std::printf("Usage: cg-anim-demo [--headless] [--output DIR] [--help]\n");
             std::printf("  --headless   Print proxy table only; skip viewer.\n");
+            std::printf("  --output DIR Write shared CSV/JSON artifacts to DIR.\n");
             return 0;
         }
     }
 
     std::printf("\n");
-    std::printf("╔══════════════════════════════════════════════════════════════╗\n");
-    std::printf("║   VSEPR-SIM  Animated Ensemble Proxy Demo                   ║\n");
-    std::printf("║   η relaxation + macroscopic proxy evolution                ║\n");
-    std::printf("╚══════════════════════════════════════════════════════════════╝\n");
+    std::printf("+==============================================================+\n");
+    std::printf("|   VSEPR-SIM  Animated Ensemble Proxy Demo                   |\n");
+    std::printf("|   η relaxation + macroscopic proxy evolution                |\n");
+    std::printf("+==============================================================+\n");
     std::printf("\n");
 
     constexpr double dt      = 10.0;
@@ -280,6 +341,14 @@ int main(int argc, char** argv) {
     print_ruler();
     std::printf("\n");
 
+    try {
+        write_demo_artifacts(output_dir, results, n_steps, dt);
+        std::printf("Shared artifacts: %s\n\n", output_dir.string().c_str());
+    } catch (const std::exception& error) {
+        std::fprintf(stderr, "Artifact export failed: %s\n", error.what());
+        return 2;
+    }
+
 #ifdef BUILD_VISUALIZATION
     if (headless) {
         std::printf("(--headless: viewer skipped)\n\n");
@@ -291,7 +360,7 @@ int main(int argc, char** argv) {
 
     for (size_t idx = 0; idx < results.size(); ++idx) {
         const auto& r = results[idx];
-        std::printf("─── Scene %zu/%zu: %s ───\n",
+        std::printf("--- Scene %zu/%zu: %s ---\n",
                     idx + 1, results.size(), r.name);
         print_proxy_row(r.name, r.proxy);
         std::printf("  Opening viewer...\n");
@@ -314,14 +383,14 @@ int main(int argc, char** argv) {
         std::printf("  Viewer closed.\n\n");
     }
 
-    std::printf("╔══════════════════════════════════════════════════════════════╗\n");
-    std::printf("║   Demo complete. All %zu scenes shown.                       ║\n",
+    std::printf("+==============================================================+\n");
+    std::printf("|   Demo complete. All %zu scenes shown.                       |\n",
                 results.size());
-    std::printf("╚══════════════════════════════════════════════════════════════╝\n\n");
+    std::printf("+==============================================================+\n\n");
 
 #else
     (void)headless;
-    std::printf("(BUILD_VIS=OFF — rebuild with cmake -DBUILD_VIS=ON for viewer)\n\n");
+    std::printf("(BUILD_VIS=OFF  -  rebuild with cmake -DBUILD_VIS=ON for viewer)\n\n");
 #endif
 
     return 0;
