# archive/

**Status: ARCHIVED — build system reference snapshots only**

This directory holds point-in-time snapshots of key project files. Nothing here
is compiled or shipped.

| File | Notes |
|------|-------|
| `CMakeLists.txt.monolithic-backup` | Original monolithic CMake file from before the two-part build split (pre-v4). Retained as a diff reference. See root `CMakeLists.txt` comment: "Original monolithic file archived here." |

**Do not modify these files.** They are frozen snapshots.

Deprecation criteria: see `CONTRIBUTING-DEPRECATION.md` in the repo root.
