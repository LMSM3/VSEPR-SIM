# V5 Known Features

## Verified v5.16.0 source features
- VSIM parser and runtime schema
- Gas-injection .xyzf trajectory writer (WO-94A)
- Terminal status loop with hardware probes (WO-93A)
- CLI `vsepr inspect <formula>` molecular database lookup
- Chrome/PDF visual fallback for missing standalone viewer
- Periodic table database and formula parser
- Atomistic state container and VSEPR classification
- Coarse-grain bead layer
- Equation-of-state and property inference stubs
- Report, dashboard, and export inventory

## Known correct outputs
- WO-94A regression test passes
- v5.16.0 source identity promoted across CMake/manifest/CLI/docs
